#!/bin/sh

mkdir -p $_CONF_INSTALL_APPLICATION_DATA_PATH

_warn "Initializing integrity database ..."

aide -c $_CONF_INTEGRITY_CONFIGURATION_FILE --init
mv ${_CONF_INTEGRITY_DATABASE_FILE}.new ${_CONF_INTEGRITY_DATABASE_FILE}

